---
# hosted_zone_id will be Filled in by library
change_batch:
  changes:
  - action: UPSERT
    resource_record_set:
      name: ${@cname}.${DNSDomain}
      type: CNAME
      ttl: ${@ttl:$CNAMERecTTL}
      resource_records:
      - value: ${@name}.${DNSDomain}
