---
# hosted_zone_id will be Filled in by library
change_batch:
  changes:
  - action: UPSERT
    resource_record_set:
      name: $@cname
      type: CNAME
      ttl: $@ttl
      resource_records:
      - value: $@name
