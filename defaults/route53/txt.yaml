---
# hosted_zone_id will be Filled in by library
change_batch:
  changes:
  - action: UPSERT
    resource_record_set:
      name: $@name
      type: TXT
      ttl: $@ttl
      resource_records: $@txtrecords
