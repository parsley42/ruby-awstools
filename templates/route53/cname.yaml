---
hosted_zone_id: ${@zone_id:$PublicDNSId}
change_batch:
  changes:
  - action: UPSERT
    resource_record_set:
      name: ${@cname}
      type: CNAME
      ttl: ${@ttl:$CNAMERecTTL}
      resource_records:
      - value: ${@name}
